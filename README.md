# Hookline

Durable webhook relay. Accept inbound webhooks, store them, and deliver to destinations with retries, backoff, idempotency, dead-lettering, and replay.

API-only for v1 (Laravel Passport). Local Docker / CI / delivery core come next.

## Status

Skeleton: Laravel 13 app, health check at `GET /up`. Not runnable as a relay yet.
